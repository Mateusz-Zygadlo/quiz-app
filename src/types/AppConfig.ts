export type AppConfig = {
  DELAY_IN_MILISECONS: number;
  TIME_IN_SECONDS: number;
  SLEEP_IN_SECONDS: number;
  THEME_MODE: "theme-mode";
  THEMES: {
    DARK_MODE: "dark-mode";
    LIGHT_MODE: "light-mode";
  };
}