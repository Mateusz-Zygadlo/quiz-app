import type { AppConfig } from "./types/AppConfig"

export const APP_CONFIG: AppConfig = {
  DELAY_IN_MILISECONS: 10,
  TIME_IN_SECONDS: 10,
  SLEEP_IN_SECONDS: 2, 
  THEME_MODE: "theme-mode",
  THEMES: {
    DARK_MODE: "dark-mode",
    LIGHT_MODE: "light-mode",
  },
}